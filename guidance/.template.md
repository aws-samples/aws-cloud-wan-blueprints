---
title: "<Cloud WAN mechanisms involved>: <the scenario, in the reader's vocabulary>"
description: "<One or two sentences on what the page concretely shows — not an abstract, not a restatement of the title.>"
---

# <Scenario heading>

<!--
This is the skeleton for a guidance page. Before writing, confirm the four-part
admission test in CONVENTIONS.md (section 6) holds; if it does not, the content
belongs on a policy/ capability page or in the SKILLS.md constraint checklist.

Rules that apply here exactly as in policy/:
- Snippets only: every fenced ```json block must parse (CI checks it), and no
  complete deployable policy documents.
- Headings lead with the mechanism (policy key, condition type, BGP attribute),
  then the scenario after a colon.
- Do not hard-wrap prose: one paragraph is one line.
- Link to the policy/ page that owns a mechanism; never re-explain it.
- One file per page: diagrams go in images/ as guidance_<page_name>_*.png,
  editable source in images/architectures.drawio. No sibling files here.

When the page is ready, add its row to the catalog in guidance/README.md and its
line to llms.txt in the same change. Do NOT add it to SKILLS.md.
-->

| | |
|---|---|
| **Applies when** | <Trigger phrases in the user's vocabulary — the symptoms and requirements an agent matches a request against, e.g. "geographic egress over Direct Connect", "all VIFs advertise the same prefixes"> |
| **Composes** | <Links to the `policy/` pages this design builds on> |
| **Test on** | <Link to the `infra/` pattern that can exercise it, and what it lacks if anything> |

## The scenario

<!-- What the user is trying to achieve, and the topology it happens in. State the
requirement in networking terms before introducing any Cloud WAN construct. End the
section by making clear why the answer spans several capabilities, or needs knowledge
outside the policy document (BGP behavior, circuit physics, symmetry) - this is the
admission test written as prose, and it justifies the page's existence. If you cannot
write that ending, the content belongs on a capability page or in the constraint
checklist instead. -->


## The design

<!-- Open with a brief overview of the complete design - the pieces and how they fit, in a few sentences - so the reader knows the destination before the walk. Then one subsection per mechanism, heading shaped as "<policy key or mechanism>: <what this step contributes>". Each subsection links to the owning policy/ page and shows only the snippet this scenario needs. -->

### <mechanism>: <what this step contributes>

```json
{
  "rule-number": 100,
  "action": "allow"
}
```

## Constraints that bite here

<!-- The subset of constraints this specific composition trips over, each linking to its source — the policy/ page or the AWS documentation. Do not restate the full checklist. -->

## Verification

<!-- How to prove the design works once deployed: what to look for in get-network-routes, what traffic to generate, and what a failure of each step looks like. -->
